# Upper bound is load-bearing: mcp 2.x removed the decorators this server uses.
mcp>=1.0.0,<2

# conscience_agent.py only. Skip if you are not running the nag.
pydantic-ai>=0.0.20
openai>=1.0.0
httpx>=0.27.0

# Optional: only needed if you set GOAL_PG to use Postgres instead of SQLite.
# psycopg[binary]>=3.1
